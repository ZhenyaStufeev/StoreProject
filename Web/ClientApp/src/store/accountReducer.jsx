import {  } from "./types.jsx";

const initialState = {

}

export default function UpdateElements(state = initialState, action={})
{
    switch(action.type)
    {
        default:
        {
            return state;
        }
    }
}